# 🤖 貸款風險預測模型 (Loan Risk Predictor Models) 科普指南

在這個專案中，我們結合了「傳統機器學習」跟「前衛的深度學習混合架構」。以下為您介紹介面中提供的 7 種不同特性的「貸款風險審查員」演算法：

---

## 🌲 傳統但強大的「樹狀」模型 (Tree-based Models)
這兩個模型是目前業界在處理「表格型資料（如：財務報表、貸款數據）」時的霸主，特色是**速度快、不用 GPU 也能跑得很好**，且通常能提供極高的準確率。

### 1. `random_forest` (隨機森林)
* **原理**：它會一次建立幾百棵「決策樹」（就像請了幾百個普通的審查員），然後遇到新貸款案時，讓這幾百人一起投票決定這筆貸款過不過。
* **優勢**：因為「人多勢眾」，綜合投票的結果非常穩定，不容易受到極端例外資料的干擾（抗過擬合能力強）。

### 2. `xgboost` (極限梯度提升)
* **原理**：同樣使用許多決策樹，但它非常聰明。它採取的策略是「循序漸進」，後面的樹會專門去學習前面那棵樹犯錯的地方。就像是一個不斷自我檢討的菁英團隊。
* **優勢**：Kaggle 數據科學比賽的常勝軍！在表格型資料上，預測準確率通常是所有單一模型中最高的。

---

## 🧠 純深度學習模型 (Deep Learning Models)
模仿人類大腦神經元的結構，適合去挖掘資料裡面「極度隱蔽、人類看不出來的非線性關聯」。

### 3. `mlp` (多層感知機 / 基本神經網路)
* **原理**：最經典的神經網路架構。它把所有貸款資料（收入、負債等）丟進一個複雜的數學黑盒子裡，經過多層神經元與權重轉換得出結論。
* **優勢**：表現中規中矩，是深度學習處理表格資料的基礎標準配備。

### 4. `rnn` (循環神經網路)
* **原理**：原本是設計來處理「有時間順序」的資料（例如股價走勢或一段對話）。把它用在貸款表格上，它會以一種「掃描條碼」的方式，按順序閱讀每一個特徵，試圖找出特徵之間的先後關聯。
* **優勢**：能夠捕捉特徵排列上的序列性模式。

### 5. `d_lstm` (深度長短期記憶網路)
* **原理**：`rnn` 的超級進化版。普通的 `rnn` 在看後面的特徵時很容易「忘記」前面的特徵；而 `lstm` 裡面有一個「閘門記憶體」機制，它可以自主決定要把「前面那個很重要的收入特徵」記住，並且跟「後面才看到的負債特徵」結合起來判斷。
* **優勢**：有效解決長距離特徵依賴的問題，記憶能力更強。

---

## 🧬 最酷的「混合型」創新模型 (Hybrid Models)
這些是把不同的演算法「縫合」在一起的超強變種，通常是學術界與前沿研究用來突破單一模型極限的作法。

### 6. `d_lstm_mlp` (LSTM 加上 MLP 混合)
* **原理**：這是一個兩階段架構。先讓聰明的 `d_lstm` 負責把雜亂的原始資料，萃取出最精華、最有代表性的內部隱含特徵（這稱為特徵萃取與降維）；接著把這些精華數據打包，交給後端的 `mlp` 神經網路做最後的「違約風險機率」綜合裁決。
* **優勢**：結合了 LSTM 萃取複雜關係的能力，以及 MLP 強大的分類判斷能力。

### 7. `cnn_lightgbm` (卷積神經網路 加上 輕量梯度提升)
* **原理**：非常前衛的設計！`cnn` 原本是拿來「辨識圖片」的（例如認出照片裡的貓）。這裡我們把貸款人的數據特徵偽裝成「圖片像素矩陣」，讓 `cnn` 像看圖一樣找出潛在的風險局部模式（Local Patterns）；然後把找出來的模式交給超強的 `LightGBM`（跟 xgboost 齊名的樹狀模型）去做最終的分類預測。
* **優勢**：以視覺化的卷積思維來解構表格特徵，再利用最強的樹狀模型做分類，是極具創意的混合模型。

---

## 💡 總結：我該怎麼選？

* **追求最高準確率與速度的實戰派**：請單選 `xgboost` 或 `random_forest`。
* **想觀賞超酷的 3D 訓練損失曲線**：請選純深度學習模型，如 `d_lstm` 或是 `mlp`。
* **想做高階的學術實驗、探索演算法極限**：請選混合型的 `d_lstm_mlp` 或 `cnn_lightgbm`！
